name: release-images

# Builds, smoke-tests and publishes the production images for a version tag.
# It holds no cloud credential and never deploys; see
# docs/design/single-host-production.md.
on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  image:
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    strategy:
      fail-fast: true
      matrix:
        name: [server, web, caddy]
        include:
          - name: server
            dockerfile: deploy/server.Dockerfile
          - name: web
            dockerfile: deploy/web.Dockerfile
          - name: caddy
            dockerfile: deploy/caddy/production/Dockerfile
    env:
      IMAGE: ghcr.io/dannyota/aboutme-${{ matrix.name }}:${{ github.ref_name }}
      NAME: ${{ matrix.name }}
      DOCKERFILE: ${{ matrix.dockerfile }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build
        run: docker build --platform linux/arm64 -f "$DOCKERFILE" -t "$IMAGE" .
      - name: Smoke
        run: |
          case "$NAME" in
            server)
              docker run --rm --entrypoint sh "$IMAGE" -c \
                'test "$(uname -m)" = aarch64 && /opt/chromium/chrome --version && test -s /etc/ssl/rds/global-bundle.pem && test -x /usr/local/bin/db-setup'
              ;;
            web)
              docker run -d --name smoke -e HOST=0.0.0.0 -e PORT=3000 -p 127.0.0.1:3000:3000 "$IMAGE"
              for _ in $(seq 1 30); do
                curl -fsS -o /dev/null http://127.0.0.1:3000/ && exit 0
                sleep 1
              done
              docker logs smoke
              exit 1
              ;;
            caddy)
              # adapt parses the full config without the runtime TLS files.
              docker run --rm --entrypoint caddy -e CLOUDFLARE_RANGES=192.0.2.0/24 "$IMAGE" \
                adapt --config /etc/caddy/Caddyfile >/dev/null
              docker run --rm --entrypoint caddy -e CLOUDFLARE_RANGES=192.0.2.0/24 "$IMAGE" \
                adapt --config /etc/caddy/Caddyfile.maintenance >/dev/null
              ;;
          esac
      - name: Install Trivy
        run: scripts/security-scan.sh install-trivy "$RUNNER_TEMP/trivy-bin"
      # Scans the image built above, before it is pushed anywhere. Fails on a
      # fixable HIGH or CRITICAL finding; see scripts/security-scan.sh. The
      # full JSON report is uploaded as a short-retention artifact instead of
      # printed, since this log is public.
      - name: Scan for fixable HIGH/CRITICAL vulnerabilities
        run: |
          scripts/security-scan.sh scan-image \
            "$RUNNER_TEMP/trivy-bin/trivy" "$IMAGE" "" "trivy-$NAME.json"
          scripts/security-scan.sh report "trivy-$NAME.json"
      - name: Upload Trivy report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: trivy-report-${{ matrix.name }}
          path: trivy-${{ matrix.name }}.json
          if-no-files-found: warn
          retention-days: 7
      - name: Push
        id: push
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ACTOR: ${{ github.actor }}
        run: |
          printf '%s' "$GHCR_TOKEN" | docker login ghcr.io -u "$ACTOR" --password-stdin
          docker push "$IMAGE"
          digest=$(docker inspect --format '{{index .RepoDigests 0}}' "$IMAGE" | cut -d@ -f2)
          echo "digest=$digest" >>"$GITHUB_OUTPUT"
          echo "$NAME=$digest" >"digest-$NAME.txt"
          echo "$NAME=$digest" >>"$GITHUB_STEP_SUMMARY"
      - uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-name: ghcr.io/dannyota/aboutme-${{ matrix.name }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: digest-${{ matrix.name }}
          path: digest-${{ matrix.name }}.txt
