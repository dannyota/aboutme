# AC-INF traceability rows

8 acceptance-criterion rows with the `AC-INF-` prefix, in their original matrix
order. See [README.md](./README.md) for the matrix purpose, maintenance rules,
and the full prefix index.

| ID         | Spec clause                                                                           | Statement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Phase/task  | Test / UAT reference                                   |
| ---------- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------ |
| AC-INF-001 | [Deployment: client-IP boundary](../../design/deployment.md#client-ip-boundary)       | Production Caddy derives the viewer address from CloudFront's validated inbound chain (trusted-proxy walk over the origin-facing prefix-list CIDRs) only after verifying a single-instance, non-empty `X-Origin-Secret` equal to a non-empty configured secret (current or next); strips all viewer-supplied forwarding headers (including reverse-proxy re-adds); emits exactly one canonical `X-Real-IP`; and refuses service on empty/unset secret or trusted-CIDR configuration (fail closed at config and entrypoint layers)                                                                                                                           | PI/7        | (pending)                                              |
| AC-INF-002 | Deployment: edge                                                                      | The Caddy-only CloudFront matrix encodes exact route cache/forwarding policy, authenticated SSE cookies, HTTPS/443/TLS 1.2 origin validation, HSTS, canonical `www` redirect before auth/origin work, and no S3 origin, Origin Access Control, or `/assets` behavior                                                                                                                                                                                                                                                                                                                                                                                        | PI/6        | Pending mocked `terraform test` and live absence probe |
| AC-INF-003 | [Deployment: environment model](../../design/deployment.md#environment-model)         | Staging and production Terraform env roots are byte-identical except backend configuration and variable values, with identical tfvars key sets; parity is CI-enforced (literal diff + key-set comparison), so the environments differ only by variables                                                                                                                                                                                                                                                                                                                                                                                                     | PI/1, PI/13 | (pending)                                              |
| AC-INF-004 | [Deployment: database and releases](../../design/deployment.md#database-and-releases) | Secret values are absent from the repository, tfvars, outputs, and Terraform state except the consuming CloudFront origin secret and staging Basic-header digest, both protected by retained SSE-KMS state, 90-day noncurrent expiry, and scoped state-read roles. Per-service execution-role SSM paths are disjoint; app task roles cannot read SSM. A dedicated migrator secret reaches only migration/bootstrap containers and the app role cannot DDL. Only the server task role may list the media bucket's `resumes/` prefix or get/put/delete `resumes/*`, and may invalidate only its one CloudFront distribution. Every other principal is denied. | PI/4        | Pending exact-policy tests and live IAM simulation     |
| AC-INF-005 | [Operations: monitoring](../../design/operations.md#monitoring)                       | The monitoring inventory — RDS storage/CPU/connections, snapshot and restore-drill failures (with missing-heartbeat treated as alarm), render queue depth/OOM interface, task readiness, TLS expiry, CloudFront 5xx — plus EIP-association-failure, deployment-circuit-breaker-rollback, and CloudFront prefix-list-drift alarms, is provisioned with dashboards and SNS on-call delivery                                                                                                                                                                                                                                                                   | PI/9        | (pending)                                              |
| AC-INF-006 | [Operations: privacy lifecycle](../../design/operations.md#privacy-lifecycle)         | EventBridge Scheduler runs enabled hourly idempotency expiry, weekly media orphan, daily privacy retention, nightly restore verification, daily origin TLS, and six-hour CIDR-drift jobs. Exact RunTask/PassRole/task policies, advisory or ownership-tag overlap locks, zero silent retries, and failure/missing-heartbeat alarms are required.                                                                                                                                                                                                                                                                                                            | PI/10       | (pending)                                              |
| AC-INF-007 | PI D25                                                                                | The staging viewer function uses runtime-2.0 SHA-256 over one Basic Authorization value, fixed-length digest comparison, exact no-store 401 challenge, and removes Authorization before origin forwarding. The digest is in SSM; plaintext exists only in protected operator/GitHub environment secrets. The same function performs canonical-host redirects before the optional gate.                                                                                                                                                                                                                                                                      | PI/6        | (pending)                                              |
| AC-INF-008 | PI D25                                                                                | Staging responses carry a blanket `X-Robots-Tag: noindex, nofollow` via the response-headers policy when the env-varying noindex variable is enabled (staging true, production false) — PI-originated control, not a spec §6 normative statement                                                                                                                                                                                                                                                                                                                                                                                                            | PI/6        | (pending)                                              |
