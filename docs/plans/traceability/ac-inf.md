# AC-INF traceability rows

10 acceptance-criterion rows with the `AC-INF-` prefix, in their original matrix
order. See [README.md](./README.md) for the matrix purpose, maintenance rules,
and the full prefix index.

These infrastructure rows describe the selected target under
[ADR 0034](../../adr/0034-scheduled-uat-and-production-autoscaling.md). Task
10.18 must resolve the replica and load-balancer mechanisms before dependent
Phase 10 dispatch. In particular, AC-INF-007's old blanket Basic-header gate is
not compatible with MCP Bearer authorization and cannot be implemented
unchanged; the resolved UAT access policy must update this row and its owning
task together. No row is newly proven by this plan revision.

Phase 9's [cost recommendation](../../research/aws-cost/recommendation.md)
supplies sizing and spending assumptions. The
[runtime handoff](../phase-10/runtime-refresh.md) records deployment gaps found
after Phase 8. These inputs do not prove any infrastructure or hosted acceptance
row; the existing Phase 10 owners and checks remain responsible.

The selected
[native ARM64 build contract](../phase-10/infrastructure/contracts.md#build-and-runner-contract)
is owned by tasks 10.8/10.12/10.13 and the infrastructure exit checklist. It
adds runtime smoke and image provenance evidence without adding an AC-INF row or
changing the existing AMD64 browser-baseline acceptance.

| ID         | Spec clause                                                                           | Statement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Phase/task                 | Test / UAT reference                                                                              |
| ---------- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------- |
| AC-INF-001 | [Deployment: client-IP boundary](../../design/deployment.md#client-ip-boundary)       | Production Caddy derives the viewer address from the validated CloudFront-to-ALB-to-Caddy chain under task 10.18's TLS, security-group, and trusted-proxy contract only after verifying a single-instance, non-empty `X-Origin-Secret` equal to a non-empty configured secret (current or next); strips all viewer-supplied forwarding headers (including reverse-proxy re-adds); emits exactly one canonical `X-Real-IP`; and refuses service on empty/unset secret or trusted-CIDR configuration (fail closed at config and entrypoint layers)                                                                                                           | 10.18, 10.7                | (pending)                                                                                         |
| AC-INF-002 | Deployment: edge                                                                      | The Caddy-only CloudFront matrix encodes exact route cache/forwarding policy, authenticated SSE cookies, HTTPS/443/TLS 1.2 origin validation, HSTS, canonical `www` redirect before auth/origin work, and no S3 origin, Origin Access Control, or `/assets` behavior                                                                                                                                                                                                                                                                                                                                                                                       | 10.6                       | Pending mocked `tofu test` and live absence probe                                                 |
| AC-INF-003 | [Deployment: environment model](../../design/deployment.md#environment-model)         | Staging and production OpenTofu env roots are byte-identical except backend configuration and variable values, with identical tfvars key sets; parity is CI-enforced (literal diff + key-set comparison), so the environments differ only by variables                                                                                                                                                                                                                                                                                                                                                                                                     | 10.1, 10.13                | (pending)                                                                                         |
| AC-INF-004 | [Deployment: database and releases](../../design/deployment.md#database-and-releases) | Secret values are absent from the repository, tfvars, outputs, and OpenTofu state except the consuming CloudFront origin secret and staging Basic-header digest, both protected by retained SSE-KMS state, 90-day noncurrent expiry, and scoped state-read roles. Per-service execution-role SSM paths are disjoint; app task roles cannot read SSM. A dedicated migrator secret reaches only migration/bootstrap containers and the app role cannot DDL. Only the server task role may list the media bucket's `resumes/` prefix or get/put/delete `resumes/*`, and may invalidate only its one CloudFront distribution. Every other principal is denied. | 10.4                       | Pending exact-policy tests and live IAM simulation                                                |
| AC-INF-005 | [Operations: monitoring](../../design/operations.md#monitoring)                       | The monitoring inventory — RDS storage/CPU/connections, snapshot and restore-drill failures (with missing-heartbeat treated as alarm), render queue depth/OOM interface, task readiness, TLS expiry, CloudFront 5xx — plus load-balancer target health, scaling failure, deployment-circuit-breaker rollback, and CloudFront prefix-list-drift alarms, is provisioned with dashboards and SNS on-call delivery                                                                                                                                                                                                                                             | 10.9                       | (pending)                                                                                         |
| AC-INF-006 | [Operations: privacy lifecycle](../../design/operations.md#privacy-lifecycle)         | EventBridge Scheduler runs enabled hourly idempotency expiry and media deletion, weekly media orphan, daily privacy retention, nightly restore verification, daily origin TLS, and six-hour CIDR-drift jobs. Exact RunTask/PassRole/task policies, advisory or ownership-tag overlap locks, zero silent retries, and failure/missing-heartbeat alarms are required.                                                                                                                                                                                                                                                                                        | 10.10                      | (pending)                                                                                         |
| AC-INF-007 | Phase 10 infrastructure D25                                                           | The staging viewer function uses runtime-2.0 SHA-256 over one Basic Authorization value, fixed-length digest comparison, exact no-store 401 challenge, and removes Authorization before origin forwarding. The digest is in SSM; plaintext exists only in protected operator/GitHub environment secrets. The same function performs canonical-host redirects before the optional gate.                                                                                                                                                                                                                                                                     | 10.6                       | (pending)                                                                                         |
| AC-INF-008 | Phase 10 infrastructure D25                                                           | Staging responses carry a blanket `X-Robots-Tag: noindex, nofollow` via the response-headers policy when the env-varying noindex variable is enabled (staging true, production false) — Phase 10 infrastructure-originated control, not a spec §6 normative statement                                                                                                                                                                                                                                                                                                                                                                                      | 10.6                       | (pending)                                                                                         |
| AC-INF-009 | [Deployment: production topology](../../design/deployment.md#production-topology)     | ECS application capacity scales out and in with minimum one and initial maximum two production replicas. Shared revocation fences and draining, account deletion, one-use print routing, SSE fanout and draining, fleet-wide per-account/IP limits and heavy-work admission, and aggregate database connections preserve the existing security and resource bounds. Hosted UAT proves 1→2→1 under concurrent work and failure.                                                                                                                                                                                                                             | 10.18, 10.5, 10.14, 10.15  | Pending runtime coordination contract, deterministic two-replica checks, and hosted scaling proof |
| AC-INF-010 | [ADR 0034](../../adr/0034-scheduled-uat-and-production-autoscaling.md)                | UAT starts and stops within booked windows and the $30/month operating ceiling. Shutdown drains work and meets privacy deadlines, prevents ECS/ASG replacement, handles RDS's seven-day automatic restart, removes the temporary load balancer, and reconciles retained charges. Resume verifies readiness and due maintenance before access. Production cannot scale to zero through UAT controls.                                                                                                                                                                                                                                                        | 10.18, 10.10, 10.14, 10.15 | Pending local lifecycle tests and hosted stop/resume/cleanup evidence                             |
