# AC-INF traceability rows

8 acceptance-criterion rows with the `AC-INF-` prefix, in their original matrix
order. See [README.md](./README.md) for the matrix purpose, maintenance rules,
and the full prefix index.

| ID         | Spec clause | Statement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Phase/task  | Test / UAT reference |
| ---------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------- |
| AC-INF-001 | §6          | Production Caddy derives the viewer address from CloudFront's validated inbound chain (trusted-proxy walk over the origin-facing prefix-list CIDRs) only after verifying a single-instance, non-empty `X-Origin-Secret` equal to a non-empty configured secret (current or next); strips all viewer-supplied forwarding headers (including reverse-proxy re-adds); emits exactly one canonical `X-Real-IP`; and refuses service on empty/unset secret or trusted-CIDR configuration (fail closed at config and entrypoint layers) | PI/7        | (pending)            |
| AC-INF-002 | §6          | The CloudFront cookie/cache/origin behavior matrix is encoded in Terraform exactly per spec §6 — cache policies, custom origin request policies (cookies forwarded only on authenticated `/api/v1/*`; viewer-supplied `X-Origin-Secret`/`X-Real-IP`/`Forwarded` never forwarded to the origin on any behavior), dual Caddy origins for the raised SSE origin-response timeout, S3 OAC for `/assets`, and a response-headers policy carrying HSTS on every behavior — and asserted by mocked `terraform test`                      | PI/6        | (pending)            |
| AC-INF-003 | §6          | Staging and production Terraform env roots are byte-identical except backend configuration and variable values, with identical tfvars key sets; parity is CI-enforced (literal diff + key-set comparison), so the environments differ only by variables                                                                                                                                                                                                                                                                           | PI/1, PI/13 | (pending)            |
| AC-INF-004 | §9          | Secret values are absent from the repository, tfvars, Terraform outputs, and Terraform state except the single documented exception (the CloudFront `X-Origin-Secret` custom origin header, mitigated by SSE-KMS state with noncurrent-version expiration and a scoped state-read role); per-service IAM SSM read paths are disjoint (server, caddy, ops roles cannot read each other's parameters; no path-wide grants)                                                                                                          | PI/4        | (pending)            |
| AC-INF-005 | §9          | The spec §9 monitoring inventory — RDS storage/CPU/connections, snapshot and restore-drill failures (with missing-heartbeat treated as alarm), render queue depth/OOM interface, task readiness, TLS expiry, CloudFront 5xx — plus EIP-association-failure, deployment-circuit-breaker-rollback, and CloudFront prefix-list-drift alarms, is provisioned with dashboards and SNS on-call delivery                                                                                                                                 | PI/9        | (pending)            |
| AC-INF-006 | §9          | Scheduled retention (interface, disabled until P8-priv) and RDS restore-verification jobs are provisioned via EventBridge Scheduler → ECS RunTask, overlap-locked (deterministic restore-instance mutex; pg advisory lock reserved for retention), with zero silent retries and failure/heartbeat alarming                                                                                                                                                                                                                        | PI/10       | (pending)            |
| AC-INF-007 | PI D25      | Staging is access-gated: a CloudFront viewer-request function enforces HTTP basic auth when the env-varying gate variable is enabled (staging true, production false; same code path), with credentials held in SSM as a hash — PI-originated control, not a spec §6 normative statement                                                                                                                                                                                                                                          | PI/6        | (pending)            |
| AC-INF-008 | PI D25      | Staging responses carry a blanket `X-Robots-Tag: noindex, nofollow` via the response-headers policy when the env-varying noindex variable is enabled (staging true, production false) — PI-originated control, not a spec §6 normative statement                                                                                                                                                                                                                                                                                  | PI/6        | (pending)            |
