# AC-UI traceability rows

Rows `AC-UI-001` through `AC-UI-006` (toolkit, three layers, field commit model,
preview without photo, accessibility and theme, test hooks) arrive with the
phase PU rebase in PV T01 as `LANDED` and are re-proven in PV T10. The rows
below own the visual identity; they become `PROVEN` only in the PV record commit
after the gates in `docs/plans/phase-pv/exit-criteria.md`.

| ID        | Design clause                              | Statement                                                                                                                                                                                                        | Phase/task | State   | Test / acceptance reference |
| --------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------- | --------------------------- |
| AC-UI-001 | Application UI; ADR 0029                       | Tailwind loads without Preflight; the chrome reset applies only under `html[data-ui="app"]` and excludes `.resume-document`, `.paged-resume`, and descendants; tokens live on the document root; the renderer golden HTML and screenshot suites pass unchanged          | PU T01/T13         | PLANNED | Toolkit stylesheet test, renderer golden and screenshot suites, exit  |
| AC-UI-002 | Application UI, three layers                   | Every surface composes `components/ui` and `components/app`; no raw button, input, select, textarea, or hand-written dialog outside the primitives, composites, renderer, crop stage, and ProseMirror root; dialogs trap focus, close on Escape, and return focus       | PU T03–T12/T13     | PLANNED | Composite suites, surface-boundary lint test, dialog focus tests      |
| AC-UI-003 | Application UI, field commit model             | Text fields commit on blur or Enter; empty removes; unchanged sends nothing; Escape reverts; enumerations, checkboxes, switches, colors, and numbers commit on change; optional customization groups are switches; editor intents in the core are unchanged             | PU T03/T08/T09/T11 | PLANNED | TextField matrix, personal-details, entry-forms, customization suites |
| AC-UI-004 | Pure renderer, preview without photo; ADR 0019 | The editor preview renders a photo-less projection while the owner photo read is loading or unavailable and shows the photo state inline; the dev seed stores no photo reference; the owner photo read never fails the key invariant on a seeded resume                 | PU T02/T07         | PLANNED | Editor-preview suite, dev-seed suite, native editor proof             |
| AC-UI-005 | Application UI; AC-EDITOR-015                  | Every page and editor panel has no serious or critical axe violation in light and dark; dialogs, menus, tabs, and the rail are keyboard operable with visible focus; reduced motion is respected; the theme cookie persists across the shell and the editor             | PU T04–T12/T13     | PLANNED | Accessibility suites, native entry and editor proofs                  |
| AC-UI-006 | Application UI, test hooks                     | Every retained `data-testid`, `data-action`, other `data-*`, `aria-label`, and visible label listed in the phase file structure resolves; `make web-e2e` and the auth, editor, MCP, entry, and public HTTPS proofs pass with only the copy changes each task file names | PU T04–T13         | PLANNED | Browser proofs, hook inventory check, exit                            |
| AC-UI-007 | Application UI; ADR 0030 tokens            | Tokens match the ADR 0030 table; `--seal` is consumed only by the seal, the state mark, and the Publish variant; the chrome font is Be Vietnam Pro; the preview sheet keeps its own background in the dark theme | PV T02     | PLANNED | Pending                     |
| AC-UI-008 | Landing and entry; ADR 0030 landing        | The landing renders the compiled-in sample resume and its seal at server render with no data fetch and the unchanged base CSP; signed-in visitors see "Open your resumes"                                        | PV T03     | PLANNED | Pending                     |
| AC-UI-009 | Application UI; publish controls           | The resume list shows each resume's publish state and canonical link from `ResumeSummary` only, up to three sheet slots, with Rename and Delete in an overflow menu                                              | PV T05     | PLANNED | Pending                     |
| AC-UI-010 | Application UI; sessions                   | Settings describes devices as browser and OS with an injected-clock relative time, and the theme cookie is honored at server render on every `/app/**` page                                                      | PV T06     | PLANNED | Pending                     |
| AC-UI-011 | Application UI; ADR 0030 module and motion | The editor top bar has exactly one `--seal` control; at 390 px the preview sheet is whole and the edit view is unclipped behind a fixed bottom switch                                                            | PV T07     | PLANNED | Pending                     |
| AC-UI-012 | Publish controls; ADR 0030 motion          | Publish success shows the stamp with the canonical link and Copy link; the title mark and preview-sheet stamp appear in one press and instantly under reduced motion                                             | PV T09     | PLANNED | Pending                     |
| AC-UI-013 | Application UI; delivery gates             | The Impeccable detector, finish review (`ship`), and documenter (`DESIGN.md`) ran on the candidate; captures exist for desktop and mobile                                                                        | PV T10     | PLANNED | Pending                     |
