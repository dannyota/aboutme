# AC-UI traceability rows

PV T10 re-proves the toolkit rows that arrived with PU and closes the visual
identity rows. Each row names the unit suites and browser proof files that carry
its evidence. The phase exit report records which commands ran on the unchanged
candidate; a file reference does not claim an unrun command passed.

| ID        | Design clause                                  | Statement                                                                                                                                                                                                                                                               | Phase/task                 | State  | Test / acceptance reference                                                                                                                                                                                                                                        |
| --------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| AC-UI-001 | Application UI; ADR 0029                       | Tailwind loads without Preflight; the chrome reset applies only under `html[data-ui="app"]` and excludes `.resume-document`, `.paged-resume`, and descendants; tokens live on the document root; the renderer golden HTML and screenshot suites pass unchanged          | PU T01/T13; PV T10         | PROVEN | `apps/web/test/ui/toolkit.test.ts` (Preflight, reset guards, root tokens); `apps/web/test/renderer/golden.test.ts`, `styles.test.ts`; `apps/web/e2e/normal-csp.spec.ts`, `print.spec.ts`, `screenshot.spec.ts`; `make web-e2e`                                     |
| AC-UI-002 | Application UI, three layers                   | Every surface composes `components/ui` and `components/app`; no raw button, input, select, textarea, or hand-written dialog outside the primitives, composites, renderer, crop stage, and ProseMirror root; dialogs trap focus, close on Escape, and return focus       | PU T03–T12/T13; PV T10     | PROVEN | `apps/web/test/ui/surface-boundary.test.ts`; `apps/web/test/app/form-dialog.test.ts`, `confirm-dialog.test.ts`; `apps/web/test/editor/editor-shell.test.ts`, `publish-dialog.test.ts`; HTTPS `entry.spec.ts`, `editor.spec.ts`, `publish.spec.ts`                  |
| AC-UI-003 | Application UI, field commit model             | Text fields commit on blur or Enter; empty removes; unchanged sends nothing; Escape reverts; enumerations, checkboxes, switches, colors, and numbers commit on change; optional customization groups are switches; editor intents in the core are unchanged             | PU T03/T08/T09/T11; PV T10 | PROVEN | `apps/web/test/app/text-field.test.ts`, `form-field.test.ts`; `apps/web/test/editor/personal-details.test.ts`, `entry-forms.test.ts`, `customization-controls.test.ts`, `date-fields.test.ts`, `publish-dialog.test.ts`; HTTPS `editor.spec.ts`, `publish.spec.ts` |
| AC-UI-004 | Pure renderer, preview without photo; ADR 0019 | The editor preview renders a photo-less projection while the owner photo read is loading or unavailable and shows the photo state inline; the dev seed stores no photo reference; the owner photo read never fails the key invariant on a seeded resume                 | PU T02/T07; PV T10         | PROVEN | `apps/web/test/editor/preview-projection.test.ts`, `editor-preview.test.ts`, `photo-controller.test.ts`; `apps/server/cmd/dev-seed/seed_test.go`; HTTPS `editor.spec.ts`, `public.spec.ts`                                                                         |
| AC-UI-005 | Application UI; AC-EDITOR-015                  | Every page and editor panel has no serious or critical axe violation in light and dark; dialogs, menus, tabs, and the rail are keyboard operable with visible focus; reduced motion is respected; the theme cookie persists across the shell and the editor             | PU T04–T12/T13; PV T10     | PROVEN | `apps/web/test/editor/accessibility.test.ts`, `editor/editor-shell.test.ts`, `editor/use-stamp.test.ts`, `sessions-settings.test.ts`, `app/theme.test.ts`; HTTPS axe scans in `entry.spec.ts`, `editor.spec.ts`, `publish.spec.ts` for both themes                 |
| AC-UI-006 | Application UI, test hooks                     | Every retained `data-testid`, `data-action`, other `data-*`, `aria-label`, and visible label listed in the phase file structure resolves; `make web-e2e` and the auth, editor, MCP, entry, and public HTTPS proofs pass with only the copy changes each task file names | PU T04–T13; PV T10         | PROVEN | `apps/web/test/ui/surface-boundary.test.ts` plus the T03–T09 page, shell, list, settings, inspector, and publish suites; `make web-e2e`; HTTPS `auth.spec.ts`, `editor.spec.ts`, `mcp.spec.ts`, `entry.spec.ts`, `public.spec.ts`, `publish.spec.ts`               |
| AC-UI-007 | Application UI; ADR 0030 tokens                | Tokens match the ADR 0030 table; `--seal` is consumed only by the seal, the state mark, and the Publish variant; the chrome font is Be Vietnam Pro; the preview sheet keeps its own background in the dark theme                                                        | PV T02/T10                 | PROVEN | `apps/web/test/app/theme.test.ts` (exact light/dark tokens and palette removal), `seal.test.ts`, `state-mark.test.ts`, and `apps/web/test/editor/editor-preview.test.ts` (dark-sheet background)                                                                   |
| AC-UI-008 | Landing and entry; ADR 0030 landing            | The landing renders the compiled-in sample resume and its seal at server render with no data fetch and the unchanged base CSP; signed-in visitors see "Open your resumes"                                                                                               | PV T03/T10                 | PROVEN | `apps/web/test/landing-sample.test.ts`, `landing.test.ts` (SSR sample, seal, copy, signed-in branch, and no-fetch source check); HTTPS `entry.spec.ts` (landing and both-theme axe); `apps/web/e2e/normal-csp.spec.ts`                                             |
| AC-UI-009 | Application UI; publish controls               | The resume list shows each resume's publish state and canonical link from `ResumeSummary` only, up to three sheet slots, with Rename and Delete in an overflow menu                                                                                                     | PV T05/T10                 | PROVEN | `apps/web/test/editor/resume-list.test.ts` (three sheets, slots, public/draft marks, hostile titles, keyboard menu, focus return) and `apps/web/test/app/relative-time.test.ts`; HTTPS `editor.spec.ts` (list, rename, and editor entry)                           |
| AC-UI-010 | Application UI; sessions                       | Settings describes devices as browser and OS with an injected-clock relative time, and the theme cookie is honored at server render on every `/app/**` page                                                                                                             | PV T06/T10                 | PROVEN | `apps/web/test/app/user-agent.test.ts`, `sessions-settings.test.ts` (descriptions, relative time, raw-UA title, capability sections, dark cookie SSR), `sessions.test.ts`; HTTPS `entry.spec.ts` (settings in both themes)                                         |
| AC-UI-011 | Application UI; ADR 0030 module and motion     | The editor top bar has exactly one `--seal` control; at 390 px the preview sheet is whole and the edit view is unclipped behind a fixed bottom switch                                                                                                                   | PV T07/T10                 | PROVEN | `apps/web/test/editor/editor-shell.test.ts` (single seal control, compact top bar, rail/tooltips, narrow switch, hostile title) and `editor-preview.test.ts` (headerless page count and 390 px whole sheet); HTTPS `editor.spec.ts`                                |
| AC-UI-012 | Publish controls; ADR 0030 motion              | Publish success shows the stamp with the canonical link and Copy link; the title mark and preview-sheet stamp appear in one press and instantly under reduced motion                                                                                                    | PV T09/T10                 | PROVEN | `apps/web/test/editor/publish-dialog.test.ts` (canonical success link, Copy link, clipboard failure, focus and P5B cases), `use-stamp.test.ts` (180/120 ms and reduced motion), `editor-shell.test.ts`, `editor-preview.test.ts`; HTTPS `publish.spec.ts`          |
| AC-UI-013 | Application UI; delivery gates                 | The repository UI gates and fresh Sol finish review (`ship`) ran on the candidate; `DESIGN.md` records the built pages; local desktop, mobile, light, and dark captures exist                                                                                           | PV T10                     | PROVEN | T10 candidate record; `.impeccable/review/{desktop,mobile,list-desktop,list-mobile,settings-desktop,editor-desktop,editor-mobile,desktop-dark,editor-dark}.png`; fresh Sol `ship` disposition; root `DESIGN.md`                                                    |
