# AC-API traceability rows

2 acceptance-criterion rows with the `AC-API-` prefix, in their original matrix
order. See [README.md](./README.md) for the matrix purpose, maintenance rules,
and the full prefix index.

| ID         | Spec clause | Statement                                                                                                                                                                                                                                                  | Phase/task | State   | Test / UAT reference                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-API-001 | §4          | The TypeScript API client is derived from `docs/api/openapi.yaml`: generated path/schema types are committed and drift-checked, a pinned typed transport consumes them, and the Nuxt client compiles and proves a representative request/response contract | P0F        | PROVEN  | `apps/web/test/api-client.test.ts` suites `generated API surface (artifact)`, `typed client (runtime contract)`, and `typed client (compile-time contract)`; `apps/web/test/nuxt/api-contract.test.ts` suite `generated API surface (consumer wiring)`; `docs/api/test/generate-wiring.test.ts` suite `generated API client wiring`; `apps/web/package.json` pins `openapi-typescript` and `openapi-fetch`, and `apps/web/app/api/client.ts` consumes the generated `paths`. `make api-check` runs the OpenAPI contract tests and non-mutating `apps/web/scripts/api-drift-check.sh`. Focused proof and the P0F/P1.1 exact-candidate integration gate pass. |
| AC-API-002 | §4          | The Dart API client is generated from `docs/api/openapi.yaml`, committed and drift-checked, and compiles against the Flutter app without weakening the shared wire contract                                                                                | Phase 12   | PLANNED | (pending)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
