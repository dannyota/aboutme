# AC-REN traceability rows

Nine acceptance criteria own renderer, template, pagination, and font
integration behavior. See [README.md](README.md) for matrix rules.

| ID         | Design clause               | Statement                                                                                                                                                                                                                       | Phase/task   | State  | Test / acceptance reference                                                                                                                                                                                                                                               |
| ---------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-REN-001 | Web: determinism            | Golden HTML is byte-stable for the `full` fixture of record × every preset × both display modes; focused tests cover draft and optional states; browser, platform, fonts, timezone, locale, clock, and random inputs are pinned | P3 T9/T11    | PROVEN | `apps/web/test/renderer/golden.test.ts` and the 80 committed HTML goldens; `apps/web/e2e/screenshot.spec.ts`; `apps/web/e2e/print.spec.ts`; `WEB_E2E_RUN_ID=p3-t11-compare-final make web-e2e` passed 64 harness cases and one normal-CSP case in the pinned AMD64 image. |
| AC-REN-002 | Web: pagination             | Continuous and editor-paged modes preserve content and order; editor page breaks occur at entry boundaries; one column renders `main` then `sidebar` without loss                                                               | P3 T6/T7/T9  | PROVEN | `apps/web/test/renderer/paginate.test.ts`, `paginate.adversarial.test.ts`, `pagination-render-parts.test.ts`, `paged-resume.ssr.test.ts`, and `paged-resume.browser.test.ts`; committed continuous and paged goldens.                                                     |
| AC-REN-003 | Fonts: coverage and loading | Fonts load locally with no third-party request; coverage labels match final bytes; the selected face and bundled fallback are awaited; the declared English, Vietnamese, and renderer fixture set never reaches a platform font | P3 T5/T11    | PROVEN | `apps/web/test/fonts.test.ts` independently parses final font bytes and catalog claims; `apps/web/e2e/fonts-offline.spec.ts` passed all 26 catalog families with external requests blocked in the pinned browser.                                                         |
| AC-REN-004 | ADR 0008                    | Presets validate; apply computes placement as a total function of current content keys, places every key exactly once, replaces the rest of customization, and never changes content                                            | P3 T8        | PROVEN | `packages/schema/test/templates.test.ts`; `apps/web/test/renderer/apply-template.adversarial.test.ts`; generated preset registry and all 20 committed preset documents.                                                                                                   |
| AC-REN-005 | Repository boundary         | The renderer cannot import editor, store, API, Nuxt runtime, clock, random, locale, or network dependencies                                                                                                                     | P3 T10       | PROVEN | `apps/web/test/import-rule.test.ts` exercises positive, negative, scope, and file-extension cases against the production ESLint configuration; `make web-lint web-typecheck web-test web-build` passed.                                                                   |
| AC-REN-006 | Web: pure renderer          | Plain `vue/server-renderer` produces deterministic output without a DOM-providing test environment or implicit runtime input                                                                                                    | P3 T6/T9     | PROVEN | `apps/web/test/renderer/sections.test.ts` renders through plain Vue SSR with no DOM; `golden.test.ts` renders every matrix cell twice and requires byte equality.                                                                                                         |
| AC-REN-007 | Templates: color and links  | Every derived role meets its per-surface contrast floor; the clamp is deterministic and non-destructive; meaningful non-text marks meet 3:1; rules meet 1.5:1; every inline link is underlined                                  | P3 T6/T8/T11 | PROVEN | `apps/web/test/renderer/clamp.test.ts`, `preset-contrast.test.ts`, `bounds.adversarial.test.ts`, `chips.test.ts`, and `plain-fields.adversarial.test.ts`; the pinned screenshot suite includes every named surface and a live 4.5:1 header-contact assertion.             |
| AC-REN-008 | Templates: optional tokens  | Page margin, surface target, and header options apply their point-of-use fallbacks without writing them into the document; invalid visual combinations degrade through the defined total functions                              | P3 T6/T8/T9  | PROVEN | `packages/schema/test/schema.test.ts`, `templates.test.ts`, `apps/web/test/renderer/styles.test.ts`, `resolve-model.test.ts`, and the 80 committed HTML goldens.                                                                                                          |
| AC-REN-009 | Fonts: document v2          | V2 font IDs equal catalog IDs; v1 stays immutable; all v1 names convert forward; every v2 family has a tested v1 fallback; presets and generated types use released IDs                                                         | P3 T5B/T8    | PROVEN | `packages/schema/test/font-v2-adversarial.test.ts`, `released-versions.test.ts`, `released-append-only.test.ts`, and `templates.test.ts`; `apps/server/internal/resume/docmigrate/v1_v2_adversarial_test.go` and `v1_v2_test.go`.                                         |
