# Phase 2B integration handoffs

Shared integration files belong to the integration owner. Workers report the
exact requested change and stop at that boundary.

## Required inputs at dispatch

- Photo and route-rate limits plus deletion-queue and orphan-sweep bounds are in
  [`../budgets.md`](../budgets.md).
- AC-MEDIA-001…009 and AC-SAVE-005 exist before dispatch.
- The design chooses private live-gated media and no direct `/assets` route.
- `Error.details`, the local S3-compatible service, and mutable resume `lng`
  metadata are adopted plan inputs.
- P3 Task 2's Go rich-text sanitizer is landed, so P2B work that depends only on
  it may proceed in parallel with the rest of P3. P3 Task 5B releases v2 and the
  real v1↔v2 converters before P2B Task 9 and Task 1's remaining version cases
  close. P3 renderer tests own context-safe escaping of plain-text fields; P2B
  preserves those fields as text and sanitizes only schema-declared rich text.

## Shared changes during execution

| File                                       | Change                                                                                                                    | State and current evidence                                                                                                                                  | Owner / downstream gate                                                               |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| Root `Makefile`                            | Add D10's exact S3 lifecycle and fail-closed media/P2B targets                                                            | **Applied:** `test-s3-up`, `test-s3-down`, `server-test-s3`, `server-test-p2b`, and `server-test-p2b-s3` are defined                                        | Integration owner; rerun through the unchanged-candidate W4 gate                      |
| `.github/workflows/ci.yml`                 | Run S3 conformance with the pinned service                                                                                | **Applied:** the `server-media-s3` job and P2B filesystem/S3 steps call the fail-closed targets                                                             | Integration owner; hosted CI remains part of the unchanged-candidate W4 gate          |
| `scripts/ci.sh`                            | Include media and resume API suites                                                                                       | **Applied:** the database path runs `server-test-p2b`; `s3_suites` runs `server-test-s3` and `server-test-p2b-s3`                                           | Integration owner; final proof is `make ci` at the unchanged candidate                |
| `packages/schema/**`                       | Supply the released-schema correction needed by the integrated P2B wire-version path without assigning schema work to P2B | **Applied by P3:** the released v1/v2 registry and generated Go contract are consumed by the production projector and P2B wire-version tests                | P3 integration owner; P2B consumes it through W4 schema/API/server gates              |
| `docs/api/openapi.yaml`                    | Land a dedicated correction if a route proves T1's contract wrong                                                         | **Downstream:** no unresolved correction is recorded; the source declares all 15 routes and the four photo operation IDs                                    | Integration owner on demand; `make api-check` and route-contract checks block W4      |
| `apps/web/app/api/generated/openapi.ts`    | Regenerate from T1's accepted OpenAPI source                                                                              | **Applied:** the generated client contains all four photo operations and the `/resumes/{id}/photo` methods                                                  | Integration owner; `make api-check` blocks W4 drift                                   |
| `apps/server/go.mod`, `apps/server/go.sum` | Apply T3's exact AWS SDK and image/text dependency report together                                                        | **Applied:** AWS SDK v2/S3, `golang.org/x/image`, and direct `golang.org/x/text` dependencies are pinned                                                    | Integration owner; server/media checks and `make ci` block W4                         |
| `../implementation-plan.md`                | Record P2B state after implementation and again after its phase review and exit gate                                      | **Applied / downstream:** it records “Implemented; exit gate active”; completion must wait for the fresh review, unchanged-candidate CI, and connected scan | Integration owner; update to complete only after every Phase 2B exit criterion passes |

`server-test-p2b` runs, from `apps/server`,
`REQUIRE_TEST_DB=1 TEST_DATABASE_URL=${TEST_DATABASE_URL:-postgres://aboutme:aboutme_dev@127.0.0.1:20432/aboutme?sslmode=disable} TEST_MEDIA_BACKEND=fs go test ./internal/resumeapi/... -race -count=1 -v`.
`server-test-p2b-s3` sources only `.dev/test-s3.env`, changes
`TEST_MEDIA_BACKEND` to `s3`, and runs the same command. `server-test-s3`
sources that file and runs
`REQUIRE_TEST_S3=1 go test ./internal/media/... -race -count=1 -v`. All three
fail when required state is absent. None starts or stops the shared database.

## Forward handoffs

| Phase   | Contract                                                                                                                                                                                                                                                                                                   | Explicit downstream gate                                                                                                                                                                                                                        |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P4      | Authenticated fetches are client-only; CSRF retry reuses the same idempotency key                                                                                                                                                                                                                          | P4's client-mutation gate must prove the retry reuses the original key and does not create a second mutation                                                                                                                                    |
| P4      | `412 details.document` is the editor rebase input                                                                                                                                                                                                                                                          | P4's editor rebase gate must drive a stale write and prove the returned current document becomes the rebase input                                                                                                                               |
| P5A     | `GET /public/resumes/{slug}/photo` applies the same live-state and generic-404 boundary                                                                                                                                                                                                                    | P5A's public-route gate must prove draft, revoked, unknown, and wrong-target photos are indistinguishable and cannot bypass Go through object storage                                                                                           |
| P5A     | Publish commands reuse the write-safety kernel; a P5A migration adds the durable singleton discovery generation, every membership mutation advances it in the same transaction after bounded lease cancellation, and public HTML uses the Caddy-denied direct Nuxt `POST /internal-render/public` contract | P5A's write/transaction gate must prove generation advancement is atomic; its routing gate must prove the internal render endpoint is unreachable through Caddy                                                                                 |
| P7A     | Chromium rendering and photo intake share the one task-wide heavy-work permit                                                                                                                                                                                                                              | P7A's resource-admission gate must contend owner rendering with photo intake and prove the configured bound, cancellation, and permit release                                                                                                   |
| P9A     | Rerun the frozen normalization corpus on the selected production ARM64 Graviton class and task cgroup                                                                                                                                                                                                      | The P9A launch gate must reproduce the manifest protocol and pass every sample within five seconds and 192 MiB RSS delta before production authorization                                                                                        |
| PI      | Production points the S3 backend at private S3; it does not add public object routing                                                                                                                                                                                                                      | PI Tasks 3 and 6 plus the PI gate must prove private bucket access by the server role, the fixed `resumes/` prefix, and no public object-store origin or route                                                                                  |
| P8-priv | Drain exact-key deletion jobs within 24 hours; reconcile remaining orphans with `ListPage`                                                                                                                                                                                                                 | P8 privacy's launch gate must prove the 24-hour job target and 48-hour paginated orphan sweep, including retry, cursor, concurrency, metric, dry-run, live-reference, and deletion-job exclusions; PI Task 10 then schedules the exact commands |
