# Phase 2A integration handoffs

These items cross P2A's owned paths. A handoff records responsibility; it does
not mark the downstream work complete.

## Owner-only shared files

| Owner             | Shared path                         | Required state                                                                                                                                                                                                           | Current evidence and next gate                                                                                                                                             |
| ----------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Integration owner | `.github/workflows/ci.yml`          | Keep the `released-schema-append-only` job beside the migration guard. Immutable released schemas may only be added; `schema-check` keeps retained generated Go and TypeScript types derived from their released schema. | Applied on `main`: the job exists, and `packages/schema/test/released-append-only.test.ts` pins it. Reverify through P2A-R2-09/11/13; no owner edit is currently required. |
| Integration owner | Root `Makefile`                     | Keep `server-test-db` fail-closed with `REQUIRE_TEST_DB=1`, `-race`, `-count=1`, and `./internal/resume/...` in the live-DB package set.                                                                                 | Applied on `main`. Reverify through P2A-R2-01/11; no owner edit is currently required.                                                                                     |
| Integration owner | `docs/plans/implementation-plan.md` | Change the P2A status only after Task 12, phase defect review, and phase acceptance pass at the same candidate commit. Keep the historical Atlas task superseded by ADR 0010.                                            | Reopened after a pre-UAT cap-trigger correction; independent review and recorded acceptance revision 3 must pass before completion.                                        |
| Integration owner | Root `go.work.sum`                  | Keep the materialized workspace sum as a tracked lockfile and inspect it as an explicit path.                                                                                                                            | Tracked on `main`; no owner edit is currently required.                                                                                                                    |

The generated-write-method restriction needs no Makefile, workflow, or custom
static-analysis edit. The phase defect reviewer must inspect it as the review
rule in [exit criteria](exit-criteria.md).

## Forward contract consumers

| Owner                               | Binding from P2A                                                                                                                                                                                                                               | Downstream gate                                                                                                                                                                           |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P2B Task 10 author                  | Implement D14 as a fixed, schema-parity-checked allowlist for customization leaf changes. Reject unknown and structural paths, including `customization.layout.sections`, without a write.                                                     | AC-SAVE-005; P2B Task 10 tests, Task 13's independent write-safety suite, and both P2B phase gates.                                                                                       |
| P2B Tasks 2 and 4 authors; P4 owner | Extend the P2A idempotency primitive to Draft v4's concrete operation identity, semantic request fingerprint, deterministic stored headers, bounded cleanup, and capacity accounting. A retry after `csrf_rejected` reuses the same key.       | AC-SAVE-002; P2B Task 4 kernel tests and Task 13's `TestIdempotency_CSRFRetryReusesKey`, followed by P4 mutation-client tests.                                                            |
| P2B Task 4 author                   | Preserve D12(ii): every granular command reads the projected aggregate, applies its change, and persists all three document parts through `resume.Store.SaveDocumentTx`. Never add an unchecked `jsonb_set` or other partial-document write.   | P2B write-kernel choke-point tests, every route test in Tasks 6–11, Task 13's independent suite, and both P2B phase gates.                                                                |
| P2B Tasks 1, 9, and 13 authors      | Add the real OpenAPI and HTTP proof for AC-SAVE-004. A declared released old version must pass `AcceptWire`, change at that wire version, return through `EmitWire`, and persist the complete current document rather than a second P2A codec. | `make api-check`; P2B Task 9 `wireversion_e2e_test.go`; Task 13 `TestWireVersion_AcceptProjectPersistEmit`; AC-SAVE-004 and AC-DOC-012 HTTP evidence; both P2B phase gates.               |
| P8 privacy owner                    | Add ADR 0016's hourly global idempotency-expiry sweep, bounded to 1,000 rows per page and 10,000 rows per run, with backlog, oldest-expired-age, and failure metrics. P2A's active-user reap is not the retention guarantee.                   | P2B Task 2 first supplies bounded cleanup and usage-counter queries. P8 privacy acceptance owns command behavior; PI Task 10 and AC-INF-006 own enabled scheduling and operational proof. |
