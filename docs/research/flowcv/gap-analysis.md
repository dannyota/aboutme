# FlowCV → aboutme gap analysis

Every capability in [`feature-inventory.md`](feature-inventory.md) checked
against the [aboutme design](../../design/README.md) and the frozen document
contract (`packages/schema/resume.schema.json`). Research evidence, not project
authority. Compiled 2026-08-11. Links below name the current successor
authority; the dated observations and verdicts are unchanged.

"Changes the frozen contract?" means: would adopting it require editing
`resume.schema.json` (and therefore the generated Go/TS types, store-layer
validation, fixtures and golden snapshots)? The
[document-version design](../../design/data.md#document-versions) allows schema
edits pre-release, so those changes are cheap now and expensive after the first
release — that timing, not the feature's charm, is why the three `v1` rows are
all contract-touching and the merely convenient ones are not.

aboutme is deliberately narrower than FlowCV. "FlowCV has it" is not a reason to
build it, and most rows below are correctly `skip`.

| Verdict | Rows |
| ------- | ---- |
| v1      | 3    |
| post-v1 | 9    |
| skip    | 62   |

## Table

| ID  | FlowCV capability                           | In aboutme design? | Where / absent                                                                                                                                         | Changes frozen contract? | Recommendation                                                                                                                                                                                                                                                                   |
| --- | ------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F01 | Multi-resume library                        | yes                | [Product journeys](../../design/product.md#core-journeys); [API endpoints](../../design/api.md#endpoint-groups)                                        | no                       | skip — already specified, capped at 3.                                                                                                                                                                                                                                           |
| F02 | Three-part document object                  | yes                | [Data: relational model](../../design/data.md#relational-model); schema root                                                                           | no                       | skip — identical decomposition.                                                                                                                                                                                                                                                  |
| F03 | Rename a resume                             | yes                | [Data: relational model](../../design/data.md#relational-model); [API endpoints](../../design/api.md#endpoint-groups)                                  | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F04 | Manual library ordering                     | no                 | absent                                                                                                                                                 | no                       | skip — ordering three items is not a feature.                                                                                                                                                                                                                                    |
| F05 | Duplicate a resume                          | no                 | absent                                                                                                                                                 | no                       | post-v1 — tailoring a variant is the main reason to hold several resumes and a copy of three jsonb columns is trivial, but it touches nothing frozen so it can land any time.                                                                                                    |
| F06 | Delete a resume                             | yes                | [API endpoints](../../design/api.md#endpoint-groups); [public namespace](../../design/product.md#public-namespace)                                     | no                       | skip — present, with a stronger slug lifecycle.                                                                                                                                                                                                                                  |
| F07 | Doc-shape version field                     | yes                | [Document versions](../../design/data.md#document-versions)                                                                                            | no                       | skip — present and better specified.                                                                                                                                                                                                                                             |
| F08 | Per-resume language field                   | yes                | [Data: relational model](../../design/data.md#relational-model)                                                                                        | no                       | skip — the column exists; nothing consumes it in v1.                                                                                                                                                                                                                             |
| F09 | Resume tags                                 | no                 | absent                                                                                                                                                 | no                       | skip — library metadata with no purpose at three resumes.                                                                                                                                                                                                                        |
| F10 | Free/paid resume cap                        | partial            | [Data: relational model](../../design/data.md#relational-model)                                                                                        | no                       | skip — AGPL product with no paid tier.                                                                                                                                                                                                                                           |
| F11 | `content` map shape                         | yes                | [Resume aggregate](../../design/data.md#resume-aggregate): ordered map; schema `content`                                                               | no                       | skip — same shape, stricter key rules.                                                                                                                                                                                                                                           |
| F12 | 15 built-in section types                   | partial            | [Resume aggregate entry table](../../design/data.md#resume-aggregate): 8 types including `custom`                                                      | yes                      | skip — `custom` (title, titleLink, subtitle, city, dates, description) already models publication, course, award, organisation, interest, reference and declaration; seven more discriminators multiply codegen, validators, fixtures and snapshots for no new expressive power. |
| F13 | Implicit section creation                   | no                 | [API endpoints](../../design/api.md#endpoint-groups): `PATCH /resumes/{id}/structure` is the only way                                                  | no                       | skip — the transactional structure endpoint exists precisely to keep the exactly-once placement invariant; implicit creation breaks it.                                                                                                                                          |
| F14 | Rename a section                            | yes                | [API endpoints](../../design/api.md#endpoint-groups); schema `displayName`                                                                             | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F15 | Section icons                               | yes                | [Resume aggregate](../../design/data.md#resume-aggregate) `iconKey`; [template section heading](../../design/templates/contract.md#53-section-heading) | no                       | skip — present; per-type defaults are editor seed data, not design.                                                                                                                                                                                                              |
| F16 | Delete a section                            | yes                | [API endpoints](../../design/api.md#endpoint-groups)                                                                                                   | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F17 | Per-layout section-order memory             | partial            | [Template columns](../../design/templates/contract.md#7-columns) render `main` then `sidebar`                                                          | no                       | skip — aboutme solves the same 1↔2 toggle problem with one placement plus a documented render order; a second stored order per layout would need its own aggregate invariant.                                                                                                    |
| F18 | Manual entry reorder                        | yes                | [API endpoints](../../design/api.md#endpoint-groups): `PATCH …/sections/{key}`                                                                         | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F19 | Auto date-sort of entries                   | no                 | absent                                                                                                                                                 | no                       | post-v1 — a genuine time-saver on a long work history, and it is a client-side sort over an already-ordered array with no schema effect.                                                                                                                                         |
| F20 | Per-entry hide                              | yes                | [Resume aggregate](../../design/data.md#resume-aggregate) `isHidden`; [publish validation](../../design/data.md#draft-and-publish-validation)          | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F21 | Delete an entry                             | yes                | [API endpoints](../../design/api.md#endpoint-groups): `DELETE …/entries/{id}`                                                                          | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F22 | Append new entries at the bottom            | yes                | implicit in the ordered `entries` array                                                                                                                | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F23 | Dates as display strings + `Present`        | no                 | [Resume aggregate](../../design/data.md#resume-aggregate) `dates`; schema `dateRange`                                                                  | n/a                      | skip — aboutme's structured range is strictly better; adopting strings would destroy sortability and break the "never fabricate a sentinel" rule.                                                                                                                                |
| F24 | Per-type rich-text field name               | yes                | [Resume aggregate entry table](../../design/data.md#resume-aggregate): `text`, `infoHtml`, `description`                                               | no                       | skip — already matches field for field.                                                                                                                                                                                                                                          |
| F25 | ProseMirror editing                         | yes                | [Product journeys](../../design/product.md#core-journeys); [realtime writes](../../design/realtime.md#writes)                                          | no                       | skip — same editor choice.                                                                                                                                                                                                                                                       |
| F26 | HTML subset (p, strong, ul, li)             | yes                | [Untrusted document content](../../design/security.md#untrusted-document-content); `validation/sanitizer-allowlist.v1.json`                            | no                       | skip — aboutme's allowlist is a superset (adds `em`, `u`, `ol`, `a`).                                                                                                                                                                                                            |
| F27 | Inline per-paragraph text alignment         | no                 | absent — allowlist has `globalAttributes: []`, `style` forbidden                                                                                       | yes                      | skip — justified body text is a minor typographic knob and an inline `style` attribute is exactly what the one-allowlist sanitizer contract exists to forbid; if ever wanted, express it as a customization key, never as inline HTML.                                           |
| F28 | Bold paragraph as sub-heading               | yes                | allowlist `strong`                                                                                                                                     | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F29 | Three placement regions (top/L/R)           | partial            | schema `layout.columns` 1\|2 with `main`/`sidebar`                                                                                                     | yes                      | skip — a full-width top band adds a third placement array and rewrites the exactly-once invariant, the structure endpoint and store validation for one layout style.                                                                                                             |
| F30 | Per-section placement                       | yes                | schema `layout.sections`; [API endpoints](../../design/api.md#endpoint-groups)                                                                         | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F31 | Template catalogue with metadata            | partial            | [Web templates](../../design/web.md#templates) as JSON presets in `packages/schema/templates/`                                                         | no                       | skip — repo presets are the deliberate v1 decision; a catalogue API only matters for user-authored templates.                                                                                                                                                                    |
| F32 | Apply template = customization swap         | yes                | [Template apply semantics](../../design/templates/contract.md#3-apply-semantics--adr-0008-is-binding)                                                  | no                       | skip — identical semantics.                                                                                                                                                                                                                                                      |
| F33 | Premium template gating                     | no                 | absent                                                                                                                                                 | no                       | skip — no paid tier exists.                                                                                                                                                                                                                                                      |
| F34 | Publish your own template                   | no                 | absent                                                                                                                                                 | no                       | skip — needs a template store, ownership and moderation; far outside v1.                                                                                                                                                                                                         |
| F35 | Entry-layout arrangement presets            | no                 | absent                                                                                                                                                 | yes                      | skip — each variant multiplies the golden-snapshot matrix (fixtures × templates) for a second-order visual gain.                                                                                                                                                                 |
| F36 | Footer: page numbers / name / email         | no                 | absent                                                                                                                                                 | yes                      | post-v1 — a name-and-page-number footer is a real multi-page print convention and cheap in CSS `@page` margin boxes, but it only appears in the PDF and the editor's approximate pagination cannot preview it faithfully.                                                        |
| F37 | Undo / redo on design changes               | no                 | absent                                                                                                                                                 | no                       | post-v1 — it protects users from wrecking a design they cannot otherwise recover, but it is editor history machinery (delta inversion), not document work.                                                                                                                       |
| F38 | Page format + date format                   | yes                | schema `pageFormat`, `dateFormat`                                                                                                                      | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F39 | Font family + serif/sans switch             | partial            | schema `font.family` (five-font enum)                                                                                                                  | no                       | skip — the fixed self-hosted set is the [font-catalog decision](../../design/web.md#font-catalog); a serif/sans filter is UI grouping over that enum.                                                                                                                            |
| F40 | Separate name/heading font                  | no                 | absent — one `font.family` for the whole document                                                                                                      | yes                      | post-v1 — one optional enum plus one CSS variable, and font pairing makes templates look genuinely different, but no CV is wrong without it.                                                                                                                                     |
| F41 | On-demand web fonts                         | no                 | [Font catalog](../../design/web.md#font-catalog): self-hosted subsets, no external font CDN                                                            | no                       | skip — aboutme's choice is deliberate (privacy, offline-deterministic print) and better.                                                                                                                                                                                         |
| F42 | Font size control                           | yes                | schema `font.baseSizePx` (10–20)                                                                                                                       | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F43 | Page margin control                         | partial            | schema `spacing` has `sectionGap`/`entryGap`/`lineHeight` only                                                                                         | yes                      | **v1** — margins are the primary lever for fitting a CV onto one page and every competing builder exposes them; one bounded number pair feeding one CSS variable, with no renderer restructuring.                                                                                |
| F44 | Base colour + accent                        | yes                | schema `colors.primary/text/background/accent`                                                                                                         | no                       | skip — present; the basic/advanced mode split is UI, not data.                                                                                                                                                                                                                   |
| F45 | Colour area (full/page/header/border)       | no                 | absent — no token expresses a filled header or sidebar band                                                                                            | yes                      | **v1** — without a fillable region every aboutme template is ink on white, so the [templates feature](../../design/web.md#templates) cannot produce visibly different designs; the change is optional colour tokens plus CSS variables.                                          |
| F46 | Heading style `line` \| `box`               | partial            | schema `heading.style` (capitalization) + `heading.showRule`                                                                                           | yes                      | post-v1 — heading treatment is a visible template axis and the change is one enum, but `showRule` already provides one decoration.                                                                                                                                               |
| F47 | Link styling control                        | no                 | absent — [template header contract](../../design/templates/contract.md#51-header) only hardens `rel`                                                   | yes                      | skip — link colour can follow the accent token; a dedicated control is decoration.                                                                                                                                                                                               |
| F48 | Per-section design overrides                | no                 | absent                                                                                                                                                 | yes                      | skip — multiplies both the customization surface and the snapshot matrix; global controls plus templates cover the need.                                                                                                                                                         |
| F49 | Customization as `{path,value}` deltas      | yes                | [API endpoints](../../design/api.md#endpoint-groups); [data invariants](../../design/data.md#bounds-and-invariants)                                    | no                       | skip — same mechanism, with an allowlist FlowCV lacks.                                                                                                                                                                                                                           |
| F50 | Whole-object personal details save          | yes                | [API endpoints](../../design/api.md#endpoint-groups): `PATCH …/personal-details`                                                                       | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F51 | Open social map with display labels         | partial            | schema `personalDetail.type` enum + `label`; only website/linkedin/github/twitter render as links                                                      | yes                      | post-v1 — academic CVs need a clickable ORCID or Scholar chip and the fix is small, but it reopens the C1 hardening decision about which chip types become `href`s, so it needs a design call first.                                                                             |
| F52 | Contact chip ordering                       | yes                | schema `personalDetails.details` array order                                                                                                           | no                       | skip — present, without a separate order field.                                                                                                                                                                                                                                  |
| F53 | Legacy website + websiteLink                | no                 | absent                                                                                                                                                 | no                       | skip — FlowCV's own legacy field, superseded by their `social` map.                                                                                                                                                                                                              |
| F54 | Header alignment / arrangement / icon style | no                 | absent — [pure renderer](../../design/web.md#pure-renderer) `ResumeHeader` takes no customization input                                                | yes                      | **v1** — the header is the top third of page one and today every template must render it identically; an alignment enum and an icon-style enum are two CSS-variable branches in one component.                                                                                   |
| F55 | Photo upload to object storage              | yes                | [Deployment media](../../design/deployment.md#media); schema `photo.key`                                                                               | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F56 | Photo shape + crop + original dims          | partial            | schema `photoCrop` fractions only; no `shape`, no source dims                                                                                          | yes                      | post-v1 — round versus square is a visible template trait, but a template-level CSS choice covers it until per-resume control is asked for.                                                                                                                                      |
| F57 | Photo show/hide toggle                      | no                 | absent — removing `personalDetails.photo` is the only "off"                                                                                            | no                       | skip — same user outcome; a toggle only preserves the crop while hidden.                                                                                                                                                                                                         |
| F58 | Fixed-width page scaled to viewport         | yes                | [Print targets](../../design/templates/print.md#1-three-targets-one-renderer): fixed 794 px, transform-scaled                                          | no                       | skip — identical approach.                                                                                                                                                                                                                                                       |
| F59 | Multi-page CSS flow from customization      | yes                | [Pagination and print](../../design/web.md#pagination-and-print); `useResumeStyles`                                                                    | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F60 | Real DOM preview (accessible text)          | yes                | [Pure renderer](../../design/web.md#pure-renderer): Vue components, props in → HTML out                                                                | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F61 | Instant zero-network preview                | yes                | [Product journeys](../../design/product.md#core-journeys); [realtime writes](../../design/realtime.md#writes)                                          | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F62 | Debounced coalesced autosave                | yes                | [Realtime writes](../../design/realtime.md#writes): ~1 s, one coalesced PATCH                                                                          | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F63 | PDF as a second, separate renderer          | partial            | [Renderer boundary](../../design/system.md#renderer-boundary): one renderer for preview, SSR, and print                                                | no                       | skip — aboutme's single renderer removes the drift risk FlowCV's two renderers carry; keep it.                                                                                                                                                                                   |
| F64 | Client-measured page count on download      | no                 | absent                                                                                                                                                 | no                       | skip — aboutme's PDF is authoritative via CSS `@page`, so a client hint would be meaningless.                                                                                                                                                                                    |
| F65 | Download your own PDF                       | yes                | [API endpoints](../../design/api.md#endpoint-groups): `GET /resumes/{id}/pdf`                                                                          | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F66 | Download via email                          | no                 | absent                                                                                                                                                 | no                       | skip — requires transactional email the product otherwise does not need.                                                                                                                                                                                                         |
| F67 | Public resume at an opaque token URL        | partial            | [Public namespace](../../design/product.md#public-namespace); [API endpoints](../../design/api.md#endpoint-groups)                                     | no                       | skip — the user-chosen global slug is deliberately richer than a token.                                                                                                                                                                                                          |
| F68 | Public PDF gated by a flag                  | yes                | [Publish controls](../../design/product.md#publish-controls); [API endpoints](../../design/api.md#endpoint-groups)                                     | no                       | skip — present.                                                                                                                                                                                                                                                                  |
| F69 | Search-index flag                           | yes                | [Publish controls](../../design/product.md#publish-controls): SEO/GEO defaults off                                                                     | no                       | skip — present and more explicit.                                                                                                                                                                                                                                                |
| F70 | Public-page cache flag                      | partial            | [Web freshness](../../design/web.md#freshness): ~60 s plus invalidation on unpublish/rename                                                            | no                       | skip — caching is infrastructure, not a per-document flag.                                                                                                                                                                                                                       |
| F71 | Feedback link on a resume                   | no                 | absent                                                                                                                                                 | no                       | skip — a comment surface is a separate product with its own moderation and privacy work.                                                                                                                                                                                         |
| F72 | No HTML snapshot (PDF only)                 | no                 | [Publish controls](../../design/product.md#publish-controls); [API endpoints](../../design/api.md#endpoint-groups)                                     | no                       | skip — aboutme is ahead here; nothing to adopt.                                                                                                                                                                                                                                  |
| F73 | Resume-as-code pull/push round trip         | partial            | [API endpoints](../../design/api.md#endpoint-groups): `GET /me/export`, granular writes                                                                | no                       | post-v1 — the write API already supports everything a push needs, and re-importing your own export closes the portability loop cheaply, but nothing in v1 depends on it.                                                                                                         |
| F74 | Markdown-space diff, last-writer-wins       | no                 | [Resume write safety](../../design/api.md#resume-write-safety): `If-Match`/412 and idempotency                                                         | no                       | skip — aboutme has real optimistic concurrency; FlowCV's note is a warning, not a feature.                                                                                                                                                                                       |

## The gaps, ranked by value to effort

Only the rows above rated `v1` or `post-v1`. Everything else is `skip`.

| Rank | ID  | Gap                                  | Verdict | Why it sits here                                                                                     |
| ---- | --- | ------------------------------------ | ------- | ---------------------------------------------------------------------------------------------------- |
| 1    | F43 | Page margin control                  | v1      | Highest-frequency user need (fit to one page) against one bounded number pair and one CSS variable.  |
| 2    | F45 | A fillable colour region             | v1      | Unblocks the entire templates feature; without it every preset is ink on white.                      |
| 3    | F54 | Header alignment and arrangement     | v1      | Second-most-visible design axis, two enums consumed by one component.                                |
| 4    | F05 | Duplicate a resume                   | post-v1 | Standard in every builder, trivial to implement, and touches nothing frozen — so it need not be now. |
| 5    | F40 | Separate name/heading font           | post-v1 | One optional enum; font pairing differentiates templates cheaply.                                    |
| 6    | F46 | `box` heading decoration             | post-v1 | One enum, but `showRule` already gives templates one decoration to vary.                             |
| 7    | F36 | Footer with page numbers             | post-v1 | Real print convention, cheap in CSS, but PDF-only and unpreviewable in the approximate editor.       |
| 8    | F51 | Linkable custom contact chip         | post-v1 | Small change that reopens a deliberate security scoping decision, so it needs a design call first.   |
| 9    | F56 | Photo shape                          | post-v1 | Visible but coverable by template CSS until per-resume control is requested.                         |
| 10   | F37 | Undo / redo on design changes        | post-v1 | Valuable safety net, but pure editor-history machinery with no document payoff.                      |
| 11   | F19 | Auto date-sort of entries            | post-v1 | Small convenience, no schema effect, easily added whenever the editor is being worked on.            |
| 12   | F73 | Import of the user's own JSON export | post-v1 | Closes the portability loop over an API that already accepts every needed write.                     |

## Where the notes and the aboutme design disagree

These matter if anyone ever imports a FlowCV document into aboutme.

- **Rich-text alignment is dropped.** FlowCV stores
  `<p style="text-align: justify">` (F27). The aboutme allowlist sets
  `globalAttributes: []` and forbids `style`, so every imported paragraph loses
  its alignment silently.
- **Dates do not survive a naive copy.** FlowCV keeps display strings and a
  `Present` sentinel (F23); aboutme requires `{start:{y,m?}, end, present}` with
  years bounded 1900–2100 and forbids fabricated sentinels, so an import must
  parse and may legitimately fail.
- **Section order lives in different places.** FlowCV keys it per column layout
  inside `customization` (F17); aboutme keys each section exactly once across
  `layout.sections.main` + `sidebar`, enforced as an aggregate invariant. There
  is no lossless mapping when a FlowCV document has different orders for the
  `one` and `two` layouts.
- **Seven FlowCV section types have no aboutme discriminator** (F12). They map
  onto `custom`, which loses the type identity (an `issuer`-style field, for
  example, becomes `subtitle`).
- **Photo shape and source dimensions have nowhere to go** (F56).
- **Internal ambiguity worth resolving in aboutme itself:** the former product
  section listed section types with a trailing ellipsis ("… project, custom…"),
  which reads open-ended, while the
  [resume aggregate's entry table](../../design/data.md#resume-aggregate) and
  `resume.schema.json` close the enum at eight. The schema is right; the former
  wording invites the wrong expectation.
